class TypeModel {
    //Parent Class For Each type model
    constructor(TypeNum, Year, Month) {
        this.TypeNum = TypeNum;
        this.Year = Year;
        this.Month = Month;
    }

    TypeCalculate() { }
}